---
/**
 * Obstetrics Page - Spanish
 * Route: /obstetricia
 */

import { Image } from 'astro:assets';
import embarazo1 from '../assets/images/embarazo1.jpg';
import embarazo2 from '../assets/images/embarazo2.jpg';
import embarazo3 from '../assets/images/embarazo3.jpeg';
import embarazo4 from '../assets/images/embarazo4.jpg';
import BaseLayout from '../layouts/BaseLayout.astro';
import Container from '../components/ui/Container.astro';
import SectionHeading from '../components/ui/SectionHeading.astro';
import Button from '../components/ui/Button.astro';
import FaqAccordion from '../components/sections/FaqAccordion.astro';
import ProcessSteps from '../components/sections/ProcessSteps.astro';
import CtaWideCard from '../components/sections/CtaWideCard.astro';
import { getTranslations, getRoute } from '../i18n';

const lang = 'es';
const t = getTranslations(lang);

// Breadcrumb structured data
const breadcrumbData = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: t.nav.home,
      item: Astro.site?.toString() || 'https://institutoginecologicoalicante.com',
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: t.obstetrics.title,
      item: `${Astro.site?.toString() || 'https://institutoginecologicoalicante.com'}obstetricia`,
    },
  ],
};
---

<BaseLayout
  title={t.obstetrics.title}
  description={t.obstetrics.description}
  lang={lang}
  pageKey="obstetrics"
  structuredData={breadcrumbData}
>
  <!-- Hero -->
  <section class="bg-gradient-to-br from-primary-50 via-white to-white py-16 md:py-24">
    <Container>
      <div class="max-w-3xl">
        <p class="text-primary font-medium text-body-sm uppercase tracking-wider mb-4">
          {t.obstetrics.hero.eyebrow}
        </p>
        <h1 class="text-display-sm md:text-display text-text font-bold mb-6">
          {t.obstetrics.hero.title}
        </h1>
        <p class="text-body-lg text-text-secondary">
          {t.obstetrics.hero.subtitle}
        </p>
      </div>
    </Container>
  </section>
<!-- Service Sections -->
  {t.obstetrics.services.map((service: { id: string; title: string; description: string; items: string[] }, index: number) => {
    const isEven = index % 2 === 0;
    const serviceImages = [embarazo1, embarazo2, embarazo3, embarazo4];
    const serviceImage = serviceImages[index]!;

    return (
      <section id={service.id} class={`py-section lg:py-section-lg ${isEven ? 'bg-white' : 'bg-gray-50'}`}>
        <Container>
          <div class={`grid lg:grid-cols-2 gap-12 lg:gap-16 items-center ${!isEven ? 'lg:flex-row-reverse' : ''}`}>
            <!-- Content -->
            <div class={!isEven ? 'lg:order-2' : ''}>
              <SectionHeading
                title={service.title}
                description={service.description}
                level={2}
                align="left"
              />

              {service.items.length > 0 && (
                <div class="mt-8">
                  <ul class="space-y-3">
                    {service.items.map((item: string) => (
                      <li class="flex items-start gap-3">
                        <span class="flex-shrink-0 w-5 h-5 mt-0.5 text-primary">
                          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                          </svg>
                        </span>
                        <span class="text-body text-text-secondary">{item}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}

              <div class="mt-8">
                <Button href={getRoute('patientCare', lang)} variant="primary">
                  {t.nav.appointment}
                </Button>
              </div>
            </div>

            <!-- Image -->
            <div class={`relative ${!isEven ? 'lg:order-1' : ''}`}>
              <div class="aspect-[4/3] rounded-2xl overflow-hidden">
                <Image src={serviceImage} alt={service.title} class="w-full h-full object-cover" loading="lazy" format="webp" />
              </div>
            </div>
          </div>
        </Container>
      </section>
    );
  })}

  <!-- Process Steps -->
  <ProcessSteps lang={lang} />

  <!-- FAQs -->
  <FaqAccordion lang={lang} faqs={t.obstetrics.faqs.items} />

  <!-- CTA -->
  <CtaWideCard lang={lang} />
</BaseLayout>
